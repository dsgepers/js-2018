
/*

    Use the following list for the exercises

 */

const list = [2,4,6];

/*

    1. Use the map method to square all even numbers from the array

 */
const result = list.map((item) => Math.pow(2, item));
console.log(result);

// const squared = list.map ...